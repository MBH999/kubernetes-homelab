apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
configMapGenerator:
  - name: default-dashboards
    files:
      - default-dashboards/15661-k8s-dashboard.json
      - default-dashboards/basic-kube-dashboard.json
    options:
      labels:
        grafana_dashboard: "1"
