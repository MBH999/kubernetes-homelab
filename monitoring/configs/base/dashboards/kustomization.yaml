apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
configMapGenerator:
  - name: default-dashboards
    files:
      - standard-dashboards/2-prometheus-stats.json
    options:
      labels:
        grafana_dashboard: "1"