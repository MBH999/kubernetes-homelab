---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: homepage
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: traefik
  hostnames: ["homepage.mbhlabs.uk"]
  rules:
    - backendRefs:
        - name: homepage
          port: 3000
