apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pgAdmin
  namespace: database
spec:
  interval: 30m
  chart:
    spec:
      chart: pgadmin4
      version: "1.50.0"
      sourceRef:
        kind: HelmRepository
        name: pgAdmin
        namespace: datanase
      interval: 12h
